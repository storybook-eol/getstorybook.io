import theme from '../../theme';

const styles = {
  container: {
    ...theme.base,
    ...theme.text,
    margin: 0,
    padding: '0 0 0 4%',
    width: '100%',
  },

  h2: {
    fontSize: 25,
    color: '#000',
    marginBottom: '30px',
    lineHeight: '28px',
  },

  content: {
    width: '95%',
  },
};

export default styles;
