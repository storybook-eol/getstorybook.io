import theme from '../../theme';

const styles = {
  container: {
    ...theme.base,
    ...theme.text,
    margin: 0,
    padding: 0,
    maxWidth: 600,
  },

  h2: {
    fontSize: 25,
    color: '#000',
    marginBottom: '30px',
    lineHeight: '28px',
  },
};

export default styles;
