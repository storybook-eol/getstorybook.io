import {stripIndent} from 'common-tags'

export default {
  id: "default-config",
  title: "Default Config",
  content: stripIndent`
    The default config is much similar to React Create App.
  `
};
